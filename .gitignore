videos
videos_test
output
work

# 個人化資源(聲音樣本例外,收在 voices/samples/)
voices/*.wav
voices/*.mp3
photos/*.png
photos/*.jpg
photos/*.jpeg

# 上傳的 PDF 可能含版權考題,不追蹤
pdfs

# slide_ingest.py 渲染出來的 PNG 可重生成,不追蹤(避免 repo 膨脹)
slides

# Gemini 錯誤調試輸出
gemini_error_*.txt

# OAuth 憑證 (publish.py 會用到)
client_secret*.json
youtube_token.json

# Python / Claude 快取
__pycache__/
*.pyc
*.pyo
work/
output/

# Server (PR-2a) runtime: job state + artifacts 不追蹤
jobs/

# React UI (PR-3e) build / deps
web/node_modules/
web/dist/
web/.vite/
web/*.tsbuildinfo
web/vite.config.d.ts
web/vite.config.js

# Claude Code harness 運行狀態 (worktrees / session state 等)
# 注意: .claude/commands/ 是專案層級 slash commands, 要追蹤, 不在這條 ignore 內
.claude/worktrees/
