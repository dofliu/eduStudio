name: autoSolverVideo
name_zh: 教學影片自動生成平台
category: edtech
progress: 99
status: active
last_updated: "2026-05-24"
next_milestone: "iter 116 完成 — server/routes/youtube.py 三個 HTTP route 從 PR-3f 上線後沒對應測試, 補 route-level 覆蓋. test_youtube_helper.py 只測 core.youtube auto_youtube_meta / _seconds_to_hhmmss 純函式, 沒覆蓋 GET /youtube_meta (DONE 短路 / deck.json 404 / existing pending overwrite) / POST /publish (UPLOADING 409 / DONE 409 / FAILED 可 retry / minimal 走預設 / 422 缺 title) / GET /youtube_status (沒上傳回 pending 空白 / 透傳 state / job 404 / 不檢 artifact / multi-artifact 跨 key 隔離) / _require_artifact path 防護 + 副檔限制. 任何 refactor 不小心動 state machine / path 防護 / DONE 短路 → 直接上線, 跟 iter 111-115 同思路 (route 安全鎖). monkeypatch _do_publish 成 noop async 避免真打 OAuth + YouTube API. TestYoutubeMetaEndpoint 5 + TestRequireArtifactValidation 4 + TestPublishEndpoint 7 + TestYoutubeStatusEndpoint 5 = 21 tests. 0 production code 改動. 1333 → 1354 tests (+21). 下一輪可推: themes.py 已 31 tests 不缺; jobs.py 內 route 邊角 (POST/GET draft / outline / approve / artifacts); proposals.py route 邊角; E2-6 / E1-4 UI 元件; E2-2 (需用戶決議 Gemini SVG)."
technologies:
  - Python 3.10+
  - FastAPI / uvicorn
  - Pydantic v2
  - React 18 + TypeScript + Vite + Tailwind CSS
  - Flask (legacy v1 review UI, 已 redirect, 過渡期保留)
  - Gemini 2.5 Flash (Vision + Text + Image)
  - PyMuPDF
  - BeautifulSoup4 (URL adapter)
  - Edge TTS / F5-TTS / Google Cloud TTS (cmn-TW Wavenet)
  - Pillow / FFmpeg (含 hardsub subtitles filter)
  - Google YouTube Data API v3 + OAuth 2.0
  - pytest + GitHub Actions CI
  - structured logging (JSONL per-job)
outputs:
  - tool
  - course
  - presentation
  - api
description: "Three input types (exam PDF / slides PDF / blog-or-doc) -> Gemini -> human review -> MP4 + SRT -> YouTube. Production-ready, content quality roadmap iterating."
description_zh: "三種輸入(考題 PDF / 教學簡報 PDF / blog 或文件)→ Gemini 處理 → 人工 review → MP4 + SRT → YouTube。production 可用, 內容品質持續迭代中。"

key_metrics: |
  測試覆蓋: 1354 tests passed, CI 4 組 matrix (ubuntu/win × py 3.10/3.12) +
  frontend-typecheck 全綠.

  --- 近期 5 個 iter (詳細歷史見 git log + docs/CHANGELOG.md) ---

  iter 116 (2026-05-24, hourly routine) — youtube.py 三個 HTTP route 補測試覆蓋:
  - server/routes/youtube.py 三個 HTTP endpoint (GET /youtube_meta /
    POST /publish / GET /youtube_status) 從 PR-3f 上線後沒對應 route-level
    測試. test_youtube_helper.py 只覆蓋 core.youtube auto_youtube_meta /
    _seconds_to_hhmmss 純函式 — 任何 refactor 不小心動 _require_artifact
    path 防護 / state machine 409 衝突 / DONE 短路 / metadata 覆蓋邏輯 →
    直接上線, 跟 iter 111-115 同思路 (route 安全鎖).
  - Mock 策略: monkeypatch server.routes.youtube._do_publish 成 noop async,
    避免 publish endpoint 走 asyncio.create_task(_do_publish(...)) 真打
    OAuth + YouTube API. route handler 在 create_task 之前已 set UPLOADING,
    可驗 state 寫入而不依賴外部 service.
  - tests/test_youtube_route.py +21 tests:
    * TestYoutubeMetaEndpoint 5: valid artifact 走 auto_youtube_meta 回 5 key
      dict / 已 DONE 直接回 existing model_dump (不重 call auto_youtube_meta,
      不該被 deck 編輯蓋掉 user 在 YT 上改過的標題) / 沒 deck.json 404 /
      pending state 帶 user 編過的 title/tags/privacy 覆蓋 auto 產物 /
      FAILED state 跟 PENDING 一樣走 overwrite (不卡 DONE 短路)
    * TestRequireArtifactValidation 4: nonexistent job 404 / `..` path
      traversal 400 / 合法 name 但檔不存在 404 / 非 .mp4 副檔 400 (q1.srt
      真存在仍擋, suffix check)
    * TestPublishEndpoint 7: 第一次 publish 202 + UPLOADING state 落盤 +
      title/tags 透傳 / UPLOADING 409 + progress 在訊息 / DONE 409 +
      video_id 在訊息 (學術影片不可逆) / FAILED 可 retry (蓋掉 FAILED,
      error 清空) / 只給 title 走預設 (desc='' / tags=[] / unlisted / 27) /
      缺 title 422 / path traversal `%2F` 不該 202
    * TestYoutubeStatusEndpoint 5: 從沒 publish 過回 pending 空白 (不 404,
      讓 UI 顯示 pending) / 有 record 透傳所有 field / job 404 (route 內
      直接 store.get, 不走 _require_artifact) / 不檢 artifact 檔存在 (
      documented: 前端在 publish 觸發瞬間就輪詢, 即使 artifact 暫時不在
      disk 仍回 pending) / multi-artifact 跨 key 隔離
  - 0 production code 改動 (純測試). 1333 → 1354 tests (+21)

  iter 115 (2026-05-24, hourly routine) — library.py route 補測試覆蓋 (helper + fields + youtube):
  - server/routes/library.py 既有 test_library_route.py 只覆蓋 iter 47
    final.mp4 priority 邏輯 (6 tests), 沒覆蓋 _read_deck_title helper
    fallback chain, 也沒覆蓋 LibraryItem 各欄位 (artifact_url / publish_url
    URL pattern / srt_exists / source_type 透傳 / mp4_size_bytes) 跟
    YoutubeUpload 透傳 (None / done / failed / uploading + multi-artifact
    跨 key 隔離). 任何 refactor 不小心把 URL pattern 改錯 / deck.json 壞
    沒退 job_id / srt 偵測寫死路徑 → 直接上線, 跟 iter 111-114 同思路.
  - tests/test_library_route.py +21 tests:
    * TestReadDeckTitle 7: no deck.json → job_id / invalid JSON → job_id
      (Exception swallow) / exam_title 優先 / deck_title fallback (repo) /
      兩 title key 都缺 → job_id / strip 前後空白 (Gemini 偶吐) / 空
      exam_title falsy 退 deck_title (current `or` chain 設計)
    * TestLibraryItemFields 9: artifact_url 格式 `/jobs/{id}/artifacts/{n}` /
      publish_url 格式 `/ui/jobs/{id}/publish/{n}` / mp4_size_bytes 透傳 /
      source_type 透傳 (document) / deck_title 從 deck.json exam_title 抓 /
      沒 deck.json 退 job_id / srt_exists True (寫 .srt 檔) / False (沒 .srt) /
      total = len(items)
    * TestLibraryYoutubeField 5: 沒上傳 → None / DONE state 透傳 video_id+url+
      title / FAILED state 透傳 error / UPLOADING state 透傳 progress_percent /
      multi-artifact 跨 key 隔離 (q1 有上傳 q2 該 None, dict[artifact_name]
      不該漏氣)
  - 0 production code 改動 (純測試). 1312 → 1333 tests (+21)

  iter 114 (2026-05-24, hourly routine) — voices.py HTTP route 補測試覆蓋:
  - server/routes/voices.py helper (_read_current_voice / _write_current_voice)
    在 test_voices.py 已有 13 個純函式測試, 但三個 HTTP route (GET /voices /
    POST /voices / GET /voices/{id}/sample) 從 PR-3l 上線後沒對應測試 — 任何
    refactor 不小心動 route 介面就直接上線. 補測 = 安全鎖, 跟 iter 111/112/113
    同思路 (slide_images / upload helper / editor 都這條路).
  - tests/test_voices_route.py +17 tests:
    * TestListVoices 5: 6 voices / current 反映 config / VoiceInfo shape
      (id / label / sample_url 該指向 /voices/{id}/sample) / config 缺檔
      fallback 第一個 voice 不該 500 / backend=f5 時 current=f5:teacher
    * TestSetVoice 8: 切 edge voice 落盤驗 backend=edge + edge.voice / 切
      f5:teacher 落盤驗 backend=f5 / f5 切換不動 f5 子區塊 (preserve
      ref_audio 等 user 設定) / unknown voice_id 400 + 不該動 config /
      pydantic v2 缺必填 voice_id 422 / SetVoiceRequest model_config 設
      extra='allow', 帶額外欄位不該 422 / POST→GET round-trip
    * TestVoiceSample 4: serves audio/mpeg byte-perfect / f5:teacher 含
      冒號透過 :path converter 解析 (普通 str converter 會把 : 視為違法,
      正是該 endpoint 用 :path 的原因) / 未知 voice 404 帶 voice_id /
      已知 voice 但 sample 檔不在 voices/samples/ 該 404 + 帶提示訊息
      不該 500
  - monkeypatch 用 server.routes.voices.{TTS_CONFIG_PATH, VOICE_SAMPLE_DIR}
    (route module 內 from-import 已捕 reference, patch core.config.* 不會
    生效, 跟 iter 111 slide_images_route 同坑)
  - 0 production code 改動 (純測試). 1295 → 1312 tests (+17)

  iter 113 (2026-05-24, hourly routine) — editor.py route 補測試覆蓋 + 揪 JS-context XSS:
  - server/routes/editor.py 從 PR-3d 上線後沒對應測試. server-side HTML route,
    user-controlled (deck_title / section.title / slide.title / narration /
    bullets / source.path) 全走原 string 拼接, 唯一防線是 _html_escape — 任何
    refactor 不小心放行 <script> / </textarea> / " 就 XSS 上線.
  - tests/test_editor_route.py +31 tests:
    * TestHtmlEscape 12: 5 種 HTML entity (& < > " ') 各驗 + & 先轉避免雙重
      escape + 空字串 / None / 0 (current behavior: falsy 一律回空) /
      非 0 int str() / 繁中字 passthrough (避免 over-escape)
    * TestRenderSlideEscape 7: XSS payload in title / narration (含
      </textarea><script> 避免 textarea 提早截斷) / bullets / value attr
      含 " / section_id 含 " (防 data-slide-id 屬性溢出) / 缺 id fallback
      (section_id_idx+1) / code_snippet HTML escape
    * TestRenderSectionEscape 4: section title XSS / section_id 含 " /
      含 slides 各渲染 / 缺 id fallback (sec{idx+1})
    * TestEditorIndex 3: empty list 走「尚無 job」 + 含 job 列表渲染
      (badge-awaiting_review 該在) + source.path 含 < / " 走 _html_escape
    * TestEditorPage 5: 不存在 job 404 / exam_pdf v1 schema 走 fallback 訊息 /
      ingesting 沒 deck.json 走「deck.json 不存在」/ deck schema 渲染 sections /
      deck XSS payload 走 HTML 渲染路徑該 escape (toolbar / section / bullet)
  - 過程中揪出 _render_editor 把 deck 透過 json.dumps(deck, ensure_ascii=False)
    直接注進 <script>const DECK = ...</script> block, 若 deck.json 任一字串
    欄位含 </script> 會打破 JS 邊界 → XSS. 已記到 docs/ROUTINE_FINDINGS.md,
    不在 routine 路徑自行修 (硬規則 #3 修 bug 前先討論). 攻擊面低 (editor.py
    只在 localhost server 對自己人開), 但仍是真實風險. 建議修法: json.dumps
    後做 `.replace("</", "<\\/")` 1 行 patch.
  - 新增 docs/ROUTINE_FINDINGS.md (新檔, 累積 routine 過程「需用戶決策才能
    修」的事項 — 不是 BLOCKED, 是「該由用戶在下個 review 視窗決定怎麼處理」
    的清單. 跟 ROUTINE_BLOCKED.md 不同, 該檔是 STOP 條件命中時才寫).
  - 0 production code 改動 (純測試 + 一份新 findings doc). 1264 → 1295 tests (+31)

  iter 112 (2026-05-24, hourly routine) — uploads.py helper unit test (path injection 安全鎖):
  - server/routes/uploads.py 內 _sanitize_filename + _unique_target_path 是
    純函式 helper, 涉及路徑安全 (path injection / Windows 保留字 / 控制字
    元). 但 test_upload.py 只測 HTTP 層 (size limit / source_type 白名單 /
    options_json 解析 / 空檔), helper 沒直接覆蓋. 任何 refactor 不小心放行
    .. / 反斜線 / null byte 就直接上線 — 安全鎖類型補測, 跟 iter 111
    slide_images route 思路一致.
  - tests/test_upload.py +21 tests:
    * TestSanitizeFilenameHappyPath 4: normal pdf / 中文 (BMP 高位區不在
      _FNAME_BAD) / 多 dot ext (archive.tar.gz rpartition 切最末) / 底線橫線
    * TestSanitizeFilenamePathInjection 6: forward slash (../../etc/passwd
      → etcpasswd) / backslash Windows-style / colon (drive letter) /
      control chars (\\x00 null byte / \\x01\\x02 / \\t) / 連續 dot collapse
      (foo..bar.pdf → foobar.pdf) / 頭尾 dot strip (...foo.pdf... → foo.pdf)
    * TestSanitizeFilenameWindowsReserved 4: CON.pdf → _CON.pdf / case
      insensitive (con.pdf / Aux.txt) / COM1-9 reserved 但 COM10+ 不在 set
      (documented) / 無 ext 時 base="" 不命中 reserved 比對 (CON 原樣 —
      documented passthrough, OS 寫檔還會擋, 不算 bug, 未來改該邏輯該主動
      更新此 test)
    * TestSanitizeFilenameFallback 3: '' / 純空白 / 只剩 dot 全 → 'upload'
    * TestUniqueTargetPath 4: 不存在直接回傳 / 已存在加 _YYYYMMDD_HHMMSS
      (驗檔名長度避免 collision) / 無 ext 走 fallback 分支整名加 _timestamp
      (結尾不該有 dot) / 多 dot ext (archive.tar_TS.gz, rpartition 切點)
  - 0 production code 改動 (純測試). 1243 → 1264 tests (+21)

  iter 111 (2026-05-23, hourly routine) — /slide_images route 補測試覆蓋:
  - server/routes/slides.py 從 PR-3h (簡報縮圖 endpoint) 上線後沒對應
    regression 測試. endpoint 本身有 path traversal 防護 + _safe_part
    過濾, 但無測試 = 任何 refactor 不小心放行 dotfile / dotdot 就直接
    上線. 補測試 = 安全鎖.
  - tests/test_slide_images_route.py +11 tests:
    * TestHappyPath 2: serves_png (byte-perfect 還原 + content-type
      image/png) + serves_multiple_pages_same_stem (同 stem 多 page
      不互相干擾)
    * TestNotFound 3: missing_stem_dir / missing_file_in_existing_stem /
      directory_instead_of_file (filename 解到目錄該 404 不該回 listing)
    * TestPathTraversalSecurity 4: dotdot_in_filename (..%2F encoded) /
      dotdot_in_stem (整段 ..%2Fetc) / backslash_in_filename (..%5C
      Windows-style) / hidden_file_dotfile_rejected (寫真 .secret_stem +
      .env 試圖讀, 仍 400 — 防 .ssh / .env 被當 slide 撈)
    * TestEdgeCases 2: empty_filename (trailing slash → 404/405/307 都
      算擋住) + special_chars_in_filename (URL %20 → 404 不該 500)
  - 純測試 (0 production 改動). monkeypatch 用
    server.routes.slides.SLIDES_DIR (route module 內 from-import 捕 reference,
    patch core.config.SLIDES_DIR 不會生效)
  - 1232 → 1243 tests (+11)

  iter 110 (2026-05-22, hourly routine) — E2-6 + E1-4 frontend bridge:
  - iter 107 (icon-suggestions) + iter 109 (image-frames) 兩個新 endpoint 上線,
    但 web/src/api.ts 還沒對應 wrapper. 未來 E2-6 / E1-4 UI iter 寫元件前,
    要先有 API client 層 — 不然 UI 元件 + API call 混在一個 commit 太大.
    這輪純橋接, 不寫 UI.
  - web/src/types.ts +4 interface:
    * IconSuggestion: 對應 backend payload 一個 IconMatch (key / icon /
      matched_keyword / position / size_ratio / domain / file_exists)
    * IconSuggestionsResponse: {suggestions: {slide_id: IconSuggestion[]}}
    * ImageFrameSummary: {count / terminal_path / has_frames}
    * ImageFramesSummaryResponse: {summary: {slide_id: ImageFrameSummary}}
  - web/src/api.ts +2 method:
    * getIconSuggestions(jobId, opts?: {maxIcons, requireFileExists}) —
      URLSearchParams 動態組 query (undefined 就走 backend default)
    * getImageFramesSummary(jobId, opts?: {requireFileExists}) — 同 pattern
  - tsc --noEmit 過, Python 1232 tests 不變 (0 Python 改動)
  - 不加新 web 測試 — 純 fetch wrapper, backend 端 (iter 107 +9 / iter 109 +8)
    已涵蓋 HTTP 行為. tsc compile 失敗就是測試.

  iter 109 (2026-05-22, hourly routine) — E1-4 backend API endpoint: GET /jobs/{id}/image-frames:
  - iter 108 開了 core.image_frames.summarize_for_deck 批次助手, 這輪把它包成
    REST endpoint, 給未來 review UI「frame preview 縮圖列」打一個 API 拿全
    deck summary (避免每 slide 一個 call). 純 Python / 0 PIL / 0 ffmpeg / 0 LLM.
    跟 iter 107 icon-suggestions endpoint 完全對稱 (同 store.deck_path pattern,
    同 404 邏輯, 同 query param 設計).
  - server/routes/jobs.py 新增 `@router.get("/{job_id}/image-frames")`:
    * Query param: require_file_exists (預設 True, 嚴格模式給 renderer;
      False 給 review UI 提案階段預覽 — frame 尚未產出來也要列在 summary)
    * 沒 deck.json → 404 (nonexistent job 同樣走這條, store.deck_path
      不會炸, 路徑不存在跟 deck 沒生兩種情境合而為一)
    * exam_pdf schema (problems, 沒 sections.slides) 自然降級 summary={}
      — 跟 summarize_for_deck 對 sections 結構的設計一致
    * 沒 image_frames 的 slide 仍保留 entry (count=0 / terminal_path=None /
      has_frames=False) — 跟「沒掃到」做出區別, UI 可顯示「這 slide 沒 frame」
    * 回應格式: {"summary": {slide_id: {count, terminal_path, has_frames}}}
      — 外層 wrap 讓未來加 metadata (例: scan_timestamp / total_count) 不
      破 API 兼容
    * summarize_for_deck 已是 JSON-friendly dict (terminal_path 是 str |
      None), 不需額外序列化轉換
  - tests/test_image_frames_endpoint.py +8 tests:
    * TestImageFramesHappyPath 3: 三 frame slide 命中 terminal_path 正確 +
      沒 image_frames 仍保留 entry (區別測試) + 跨多 section 全 slide 都
      在 summary 出現 (含 image_frames=None / 缺 key 混合場景)
    * TestImageFramesExamSchema 1: exam_pdf problems schema → {}
    * TestImageFramesErrors 2: no deck.json 404 / nonexistent job 404
    * TestImageFramesQueryParams 2: require_file_exists=true 缺檔被過濾 +
      false 缺檔仍回 (terminal_path 是缺檔路徑) / empty sections={} 防呆
  - 1224 → 1232 tests (+8)

  iter 108 (2026-05-22, hourly routine) — E1-4 backend slice: image_frames.summarize_for_deck:
  - E1-4 (review UI frame preview 縮圖列) 的 backend 第一步, 跟 iter 106
    icon_picker.suggest_for_deck 對稱. 給未來 review UI / API endpoint 一
    次拿全 deck frame summary (避免每 slide 一個 API call).
  - 新增 `core.image_frames.summarize_for_deck(deck, *, require_file_exists=True)`:
    * 內部對每 section / slide 跑 valid_frames, 取 len + terminal path
    * 回 `{slide_id: {count, terminal_path, has_frames}}`, 純 JSON 友善
    * 缺 image_frames key / image_frames=None / [] → 仍保留 summary 但
      count=0 / terminal_path=None / has_frames=False (跟「沒掃到」做出
      區別 — UI 可顯示「這 slide 沒 frame」狀態)
    * 缺 id 的 slide 跳過 (normalize_deck 後不該發生, 但 defensive)
    * 空 sections / 缺 sections key / 空 slides 全 graceful, 0 KeyError
    * 跟 valid_frames 同樣的 graceful fallback (invalid entry / 缺檔 →
      過濾, summary count 反映過濾後結果)
    * require_file_exists 透傳 valid_frames: 嚴格模式 (renderer 用) vs
      寬鬆模式 (review UI 提案階段顯示「將會」算進 frame 的 entry)
  - tests/test_image_frames.py +9 tests (TestSummarizeForDeck class):
    * empty_deck / missing_sections / single_slide_with_frames /
      multi_section_preserves_all_ids (含 image_frames=None / 缺 key 場景) /
      slide_missing_id_is_skipped (缺 id + 空 id 兩種) /
      terminal_path_is_highest_ratio_frame (亂序輸入也排對) /
      require_file_exists_false_includes_missing (嚴格 vs 寬鬆對照) /
      section_with_no_slides_yields_no_entries /
      filters_invalid_frame_entries (缺 path / ratio 越界透過 valid_frames 過濾)
  - 1215 → 1224 tests (+9)

  iter 107 (2026-05-22, hourly routine) — E2-6 backend API endpoint: GET /jobs/{id}/icon-suggestions:
  - iter 106 開了 core.icon_picker.suggest_for_deck 批次助手, 這輪把它包成
    REST endpoint, 給未來 review UI「自動建議 icon 勾選列」打一個 API 拿
    全 deck 建議 (避免每 slide 一個 call). 純 Python / 0 LLM / 0 cost.
  - server/routes/jobs.py 新增 `@router.get("/{job_id}/icon-suggestions")`:
    * Query params: require_file_exists (預設 True, False 給 UI 提案預覽
      — 顯示「將會」建議的 icon, 渲染前再過濾) + max_icons (預設 3,
      ge=1 le=20 FastAPI Query 範圍驗證, 越界 422)
    * 沒 deck.json → 404 (nonexistent job 同樣走這條, store.deck_path
      不會炸, 路徑不存在跟 deck 沒生兩種情境合而為一)
    * IconMatch dataclass 序列化: icon_path (PosixPath / WindowsPath) →
      str 給前端拿 URL 用. 其他欄位 (key / matched_keyword / position /
      size_ratio / domain / file_exists) 原樣透傳
    * exam_pdf schema (problems, 沒 sections.slides) 自然降級 suggestions={}
      — 跟 suggest_for_deck 對 sections 結構的設計一致 (不需特別 dispatch)
    * 沒命中也保留 slide_id=[] (繼承 suggest_for_deck 設計, 給 UI「這
      slide 沒建議」狀態, 跟「這 slide 沒掃到」做出區別)
    * 回應格式: {"suggestions": {slide_id: [IconMatch dict, ...]}} —
      外層 wrap 讓未來加 metadata (例: scan_timestamp / total_count) 不
      破 API 兼容
    * 走 store.deck_path 既有 pattern, 跟 GET /draft + GET /outline 一致
  - tests/test_icon_suggestions_endpoint.py +9 tests:
    * TestIconSuggestionsHappyPath 2: matching narration 命中正確 (key /
      matched_keyword / file_exists / icon path 是 str) + slide 沒命中
      仍保留 key=[] (區別測試)
    * TestIconSuggestionsExamSchema 1: exam_pdf problems schema → {}
    * TestIconSuggestionsErrors 4: no deck.json 404 / nonexistent job
      404 / max_icons=0 422 / max_icons=21 422
    * TestIconSuggestionsQueryParams 2: max_icons=1 caps 同 narration
      多命中只回一個 + require_file_exists=false 仍回缺檔 entry 帶
      file_exists=False
  - fake_icon_library fixture 用 monkeypatch ICON_LIBRARY_ROOT +
    MANIFEST_PATH, 隔離真 assets/icon_library/ 內容
  - 1206 → 1215 tests (+9)

  iter 106 (2026-05-22, hourly routine) — E2-6 backend slice: icon_picker.suggest_for_deck:
  - E2-6 (review UI 自動建議 icon 勾選列) 的 backend 第一步, 把 iter 99
    pick_icons 的單 narration 介面拉到 deck 層級. 給未來 review UI / API
    endpoint 一次拿全 deck 建議 (避免每 slide 一個 API call).
  - 新增 `core.icon_picker.suggest_for_deck(deck, **kwargs)`:
    * 內部對每 section / slide 跑 pick_icons, kwargs 全透傳
    * 回 `{slide_id: list[IconMatch]}`, 沒命中也保留 key=[] (跟「沒掃到」
      做出區別 — UI 可顯示「這 slide 沒建議」狀態)
    * 缺 id 的 slide 跳過 (normalize_deck 後不該發生, 但 defensive)
    * 空 sections / 缺 sections key / 空 slides 全 graceful, 0 KeyError
    * 跟 pick_icons 同樣的 graceful fallback (SVG 不存在 → 該 entry 跳過)
  - tests/test_icon_picker.py +8 tests (TestSuggestForDeck class):
    * empty_deck / missing_sections / single_slide_with_match /
      multi_section_multi_slide_preserves_all_ids / slide_missing_id_skipped /
      empty_narration_slide_returns_empty_list / kwargs_passthrough_to_pick_icons /
      section_with_no_slides_yields_no_entries
  - 1198 → 1206 tests (+8)

  iter 105 (2026-05-22, hourly routine) — SlideRenderer _resolve_step_bg helper DRY:
  - iter 104 把 image_frames terminal_frame fallback 落地進 SlideRenderer 兩
    layout (_render_full + _render_split_left), 兩處各 copy 一份「lift import
    + tf = terminal_frame(...) + 若有蓋 bg_rel + _resolve_asset」5 行邏輯.
    日後新增 layout (split-right / center-stage / 等) 漏接 image_frames 是個
    高機率 bug source — 走 SlideRenderer 但沒 frame fallback 體驗會不一致.
  - 抽 @staticmethod _resolve_step_bg(step, step_idx) → (bg_rel, bg_path | None).
    集中 terminal_frame call + bg_rel 解析 + _resolve_asset, 兩 layout 從 5
    行縮成 1 行 `bg_rel, bg_path = self._resolve_step_bg(step, step_idx)`.
  - lift `from core.image_frames import terminal_frame` 進 module-level import
    block (跟 core.visuals 同段), 不再每次 _render_* call 都 import.
  - 過渡期 debug log: 當 image_frames 真正覆蓋 bg_image 時印一行
    "[frame NNN] 🎞 image_frames 終端 frame 覆蓋 bg_image: <path>",
    方便除錯時一眼看出當前 bg 來源是 bg_image 還是 frame.
  - tests/test_slide_renderer.py +9 tests (TestResolveStepBg class):
    * no_image_frames_uses_bg_image (基本透傳)
    * empty_bg_image_returns_none_path (bg_image="" 防呆)
    * missing_bg_image_key_returns_none_path (連 key 都沒, 預設 "")
    * image_frames_terminal_overrides_bg_image (核心覆蓋路徑)
    * invalid_image_frames_falls_back_to_bg_image (缺檔 + 越界全無效 → 退 bg_image)
    * none_image_frames_uses_bg_image (image_frames=None 明確 noop)
    * image_frames_only_no_bg_image (沒 bg_image 仍能拿 frame)
    * override_prints_debug_line (capsys 驗 log 含 step_idx zfill 3 + path)
    * no_override_no_debug_line (沒蓋時不噴 log, 避免 noise)
  - 1189 → 1198 tests (+9)

  iter 104 (2026-05-22, hourly routine) — E1-2 image_frames 解析器 + SlideRenderer 終端 frame fallback:
  - 動態視覺素材 RFC Phase 1 E1 落地最後一步前置. iter 101 開了
    slide.image_frames schema 透傳 (E1-1), 這輪解析 + 起頭給 renderer 用.
  - 新模組 core/image_frames.py (純函式, 0 PIL / 0 ffmpeg 依賴):
    * valid_frames(): 過濾非 dict / 缺 path / ratio 越界 / 缺檔, 依
      display_ratio 升冪排序, 同 ratio 後覆蓋
    * select_frame(progress 0~1): 對應「(prev_ratio, current_ratio]」
      半開區段; 給未來 build_clip 真 frame 序列用
    * terminal_frame(): 取 display_ratio 最大那筆; 給「一步一張 PNG」
      過渡期 SlideRenderer 拿最終 frame 當靜態圖 (敘述跑完留下完整圖)
    * frame_count(): review UI 顯示「這頁有 N frame」用
    * require_file_exists toggle: renderer True, review UI 預覽 False
  - pipeline.py SlideRenderer 兩 layout (_render_full + _render_split_left)
    都接 terminal_frame — step.image_frames 若有效, terminal frame 的
    path 覆蓋 bg_image. build_clip 還沒接 frame 序列前的過渡 (E1-3 +
    build_clip refactor 才動真 frame 動畫).
  - 1162 → 1189 tests (+27):
    * TestValidFrames 11: None / 非 list / 空 / 過濾 / 排序 / 越界 ratio /
      缺 path / 非 dict / 非數值 ratio / 重複 / 缺檔 require=True/False
    * TestSelectFrame 8: None / 各區段對應 / clamp 越界 progress / 邊界
      inclusive
    * TestTerminalFrame 4: None / 取最高 ratio / 單 frame / 全 invalid
    * TestFrameCount 3: None / 過濾邏輯 / review 模式

  iter 103 (2026-05-22, hourly routine) — E2-5 ext compose_icons 三大 renderer 全覆蓋:
  - iter 102 只接 SlideRenderer (_render_full + _render_split_left), STATUS 留
    「下輪可推 E2-7 (PptxStyleRenderer / BlackboardRenderer 也接)」. 這輪收尾.
  - pipeline.py BlackboardRenderer.render — chalk 內容 + svg/image panel 畫完
    後接 compose_icons(data["steps"][step_idx-1].get("icon_overlay")), 取當前
    最新可見 step (跟 SlideRenderer 「只當前 step 的 icon」pattern 一致, 避免
    多 step 累積 icon 視覺擁擠). canvas_h=CONTENT_BOTTOM.
  - core/render/pptx_style.py PptxStyleRenderer.render — 4 路 layout (cover /
    outro / short_video / normal) 都接 self._compose_icon_overlay(img, step).
    新私有 helper 集中 import 跟 canvas_h=CONTENT_BOTTOM 一處, 4 路不重複.
  - tests/test_icon_overlay.py +8 tests:
    * TestBlackboardRendererIntegration 3: renders_with_icon (top-right 紅 pixel
      驗) / no_icon_overlay_still_works (backwards compat) /
      icon_canvas_h_respects_subtitle_band (字幕帶不被染紅)
    * TestPptxStyleRendererIntegration 5: normal/cover/outro 3 路 layout 各驗
      top-right 紅 pixel + no_icon_overlay backwards compat + bottom-right 不
      入字幕帶. (short_video layout 因需更多 step 欄位暫不覆蓋, 結構一致風險低)
  - 1154 → 1162 tests (+8)

  iter 102 (2026-05-22, hourly routine) — E2-5 icon_overlay PIL composite:
  - 動態視覺素材 RFC Phase 1 E2 端到端打通最後一步: iter 99 算 IconMatch list,
    iter 100 schema 開 slide.icon_overlay 透傳, 這輪渲染端接上.
  - 跟 photo_overlay.py 同 pattern (純 Pillow / 任何例外靜默 swallow /
    不擋整批渲染).
  - 新增 core/icon_overlay.py — compose_icons(img, icons, canvas_w, canvas_h)
    + _resolve_position 五個位置 (top-left/right, bottom-left/right, center)
    + _load_icon (.png 直開; .svg 同層級有 .png 就用; .svg 純檔 graceful skip
    等 E2-2 + cairosvg). size_ratio clamp 0.02~0.50 防呆.
  - pipeline.py SlideRenderer._render_full / _render_split_left 兩 layout
    都接 compose_icons, canvas_h 傳 visible_h (扣字幕帶, 避免 bottom-* icon
    掉到字幕區).
  - 暫不動 PptxStyleRenderer / BlackboardRenderer (一輪 ≤3 檔範圍紀律,
    下輪可推 E2-7).
  - 1135 → 1154 tests (+19): TestNoOpInputs 6 / TestSVGFallback 3 /
    TestPositionResolver 6 / TestSizeAndMultiple 4

  iter 101 (2026-05-22, hourly routine) — E1-1 slide.image_frames schema 欄位:
  - 動態視覺素材 RFC Phase 1 E1 (PNG frame 序列, 流程圖/架構圖漸進顯示) 起點,
    跟 iter 100 E2-4 對稱 — 都是純 schema 欄位透傳, 渲染端待後續 iter.
  - core/deck.py normalize_deck: slide.image_frames 預設 None (舊 deck 相容,
    既有 deck.json 不該炸)
  - core/deck.py deck_to_exam_schema_pptx: step 透傳 image_frames (pptx 主軌)
  - core/deck.py deck_to_exam_schema_slides: step 也透傳 (review UI 共用)
  - 預期 dict 欄位: path (str, PNG 路徑) + display_ratio (float 0.0~1.0
    累進佔比). 此處純透傳, 不檢驗內部結構 — E1-2 renderer 接時用 .get() 容錯.
  - tests/test_deck.py +6 tests: normalize 預設 None + 既有保留, pptx 透傳 +
    缺欄位預設 None, slides 透傳 + 缺欄位預設 None
  - 1129 → 1135 tests (+6)

  iter 100 (2026-05-22, hourly routine) — E2-4 slide.icon_overlay schema 欄位:
  - 動態視覺素材 RFC Phase 1 第三步, 接 iter 99 icon_picker. E2-3 算出 IconMatch
    list, 但 deck schema 沒地方放 — 渲染端 (E2-5) / review UI (E2-6) 拿不到.
  - core/deck.py normalize_deck: slide.icon_overlay 預設 None (舊 deck 相容,
    既有 deck.json 不該炸)
  - core/deck.py deck_to_exam_schema_pptx: step 透傳 icon_overlay (pptx 渲染主軌)
  - core/deck.py deck_to_exam_schema_slides: step 也透傳 (slides_pdf 跟 pptx 共用
    review UI, icon 自動建議邏輯一致)
  - 純透傳, 不檢驗 dict 內部結構 — E2-5 renderer 接時用 .get() 容錯
    (預期欄位: path / position / size_ratio / 選擇性 start_ms / duration_ms)
  - tests/test_deck.py +6 tests: normalize 預設 None + 既有保留, pptx 透傳 +
    缺欄位預設 None, slides 透傳 + 缺欄位預設 None
  - 1123 → 1129 tests (+6)

  iter 99 (2026-05-22, hourly routine) — E2-3 icon_picker keyword grep 模組:
  - 動態視覺素材 RFC Phase 1 第二步, 接 iter 98 manifest 框架
  - 新增 core/icon_picker.py — load_manifest() + pick_icons(narration, ...).
    純 Python grep, 0 LLM call, 0 cost. 設計細節:
    * IconMatch dataclass: key / icon_path / matched_keyword (給可解釋性) /
      position / size_ratio / domain / file_exists
    * graceful fallback: SVG 檔不存在 (E2-2 未產) 該 entry 跳過, 不噴 error
    * case-insensitive substring match: "pid" 命中 manifest "PID"
    * max_icons 預設 3 (避免畫面太雜), 同 entry dedup
    * require_file_exists 預設 True; False 給 review UI 提案階段顯示用
  - 新增 tests/test_icon_picker.py — 17 tests, 用 tmp_path 建假 manifest +
    假 svg 隔離測試, 不依賴真 assets/icon_library/ 內容. cover: 空 narration
    / None 防呆 / dedup / max_icons cap / graceful fallback (兩個方向) /
    case-insensitive / IconMatch 欄位 / 真 manifest schema 不漂移
  - 1106 → 1123 tests (+17)

  iter 98 (2026-05-22, hourly routine) — E2-1 icon library manifest 框架:
  - 動態視覺素材 RFC Phase 1 起點 (2026-05-22 用戶決議 E2 走候選 A keyword grep)
  - 新增 assets/icon_library/manifest.json — 25 entry (10 generic + 5 wind +
    5 control + 5 mechanics) 含 keywords / position / size_ratio / domain
  - 新增 assets/icon_library/README.md — 欄位說明 + 加新 icon 流程
  - 新增 tests/test_icon_library_manifest.py — 16 tests (schema / RFC 數量
    對齊 / position 合法值 / size_ratio 範圍 / icon path 跟 domain 一致 /
    無重複 keyword 跨 entry)
  - SVG 檔尚未產 (E2-2 待 Gemini 一次性產, routine 下輪可推), 在 SVG 進來
    之前 icon_picker 該 graceful fallback (E2-3 task)
  - 1090 → 1106 tests (+16)

  iter 97 (2026-05-22, hourly routine 解 dirty tree blocker) — UI redesign 串接:
  - 卡狀: 連續多輪 hourly routine 命中 SOP Step 1 STOP (dirty tree),
    docs/ROUTINE_BLOCKED.md 累積 4 條紀錄沒推進
  - 用戶授權處理. 改動分三類:
    1) web/* 6 檔 UI redesign 第二階段串接 — App.tsx header → Sidebar
       layout, JobsIndex.tsx 改兩欄 list+detail (邏輯保留 polling/filter),
       Library.tsx grid/list view 切換, index.css 加 paper-grain /
       chalk-surface / scrollbar-thin / .num / .font-display, tailwind
       config 加 forest 50-900 色階 + paper/accent + Instrument Serif /
       IBM Plex Sans / JetBrains Mono Google Fonts. 串接 iter 87d (b0016ee)
       已 commit 的 ui/Sidebar/Topbar/Btn/Field/Meter 等元件.
    2) tests/trst.py 個人 TTS 句子微調 → restore (不該進 main)
    3) .gitignore 補本機產物: tts_samples/ / _scriptor_errors/ /
       *.zip / *.rar / docs/*.mp4 (備份打包 / scriptor 失敗 dump / 影片產物)
  - 驗證: tsc --noEmit 過, pytest 1090 全綠 (web only 不動 Python)
  - 刪 docs/ROUTINE_BLOCKED.md, routine 下輪可繼續 backlog

  iter 96 (2026-05-22, hourly routine 首輪) — docs/skills.md 新增:
  - 把 .claude/skills/ 設計理念 (跟 repo 走, 不在 ~/.claude/skills/)
    + 現況索引表 (pdf-to-video ✅ / video-to-youtube ⛔)
    + pdf-to-video 流程 + 硬規則 + video-to-youtube 為何沒做 (OAuth
    安全模型 3 方案 trade-off) + 自寫 skill 給 Kiwi/Christian 指引
    寫進一份 README. 純 docs 不動 code, tests 不變 (1090 pass).

  iter 94 (2026-05-17, commit 0ca58e5) — talking_head hotfix:
  - 用戶實測 talking_head=off 影片仍有頭像
  - 根因: pipeline_config.json dynamic_avatar.enabled=true 走 build_clip
    ffmpeg overlay 路徑, 完全不經過 overlay_teacher_photo
  - 修: build_clip 開頭 import _should_skip_by_runtime_override, 命中
    強制 dyn={} disable. +1 整合測試 (mock subprocess + 驗 ffmpeg cmd)

  iter 93 (2026-05-17, commit a72b1b7) — TTS 公式 normalize:
  - 用戶實測 GCP Wavenet 念糟: backtick 念「上句點」, uP(t) 念 "up t",
    e(t) 念 "et"
  - 後處理 (tts_backend.normalize_text 全 backend 受惠):
    剝 markdown / 兩字母拆字 / 函式記法轉 "of"
  - 前處理 (scriptor_*.txt 兩 prompt): 嚴禁 narration 用 backtick/星號
  - +18 tests/test_normalize_text_formula

  iter 92b (2026-05-17, commit 448b0b7) — GCP voice 命名 hotfix:
  - 實測 zh-TW-Wavenet-A 已 404, 2026 GCP 改 cmn-* prefix
  - 修 tts_backend / docs / tests 一致
  - 新增 tests/list_voices.py 供將來查清單

  iter 92 (2026-05-17, commit d1da48f) — 內容品質三軸升級:
  - talking_head 三段選項 (always / long_form_only / off)
  - L2 narration_style 5 preset + L3 persona jliu v1
  - Google Cloud TTS provider (FallbackTTS 機制)
  - +41 tests

  iter 91 (2026-05-17, commit eac3ec2) — 中文 wrap 標點 + 孤字後處理:
  - 短影片實測「Skill 的本質與差/異」孤字落單
  - _balance_wrap_lines 三輪修整 (開頭標點推下 / 收尾標點拉回 / 末行借字)
  - +8 tests

  --- Major milestones ---

  - v3.0 (PR-1 ~ PR-3e): 平台基礎, 5 種 source, 3 種 renderer, REST API + React UI
  - v3.1 (PR-3f ~ PR-3m): 平台合一, YouTube 上傳 / 考卷編輯 / Library
  - v3.2 (PR-4a ~ PR-4c): pytest + CI / per-job logging / 單章重 render
  - v3.3 (PR-5a ~ 5c): Navy 主題 / F5 預切句 / 燒字幕
  - v4 暖身 (iter 1~22): Docker + ideate + 工程圖 AI scaffold
  - 內容品質 roadmap A→D (iter 73~91): 主題系統 / 9:16 短影片 / 自訂色票 /
    narration 長度驗證 / outline preview / 字幕風格
  - 用戶實測反饋三輪 (iter 87~94): CI 全綠修復 / 短影片 layout / proposal
    duplicate / wrap 後處理 / 三軸內容升級 (頭像/風格/GCP TTS) / TTS 公式

  --- 等用戶介入 (詳細 backlog 見 TODO.md) ---

  - 詞句 / 發音對照 (待用戶列念錯詞)
  - narration_style 5 preset 選定
  - persona/jliu v2 樣本
  - voice clone (ElevenLabs Instant) 決定
  - C2 F5 pronunciation (主軌已換 GCP, 看是否還維護 F5)

  --- Routine handoff (2026-05-17) ---

  iter 95+ 開始, 移交 Anthropic 雲端 routine 每小時 fire 一次自主推 backlog.
  SOP: docs/ROUTINE_ADVANCE_PROMPT.md
  STOP 條件 + 硬規則寫進 SOP, 不可繞.
